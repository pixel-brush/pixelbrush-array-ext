{
    "name": "pixelbrush-ts-array-ext",
    "version": "0.0.3",
    "description": "A set of typescript array extensions",
    "main": "dist/index.js",
    "typings": "dist/index.d.ts",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "build": "rimraf ./dist && mkdir dist && cp ./lib/index.d.ts ./dist && parcel build ./lib/index.ts"
    },
    "author": "danny@pixelbru.sh",
    "license": "ISC",
    "devDependencies": {
        "parcel": "^1.12.4",
        "rimraf": "^3.0.0",
        "typescript": "^3.6.4"
    }
}
